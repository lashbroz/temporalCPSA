% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/age-segmentation.R
\name{ageTMP_segment_diagnostic_matrix}
\alias{ageTMP_segment_diagnostic_matrix}
\title{Build an ordered segment diagnostic matrix}
\usage{
ageTMP_segment_diagnostic_matrix(
  tmp_matrix,
  sample_age,
  k_values = 2:6,
  min_n = 10,
  scale_rows = FALSE
)
}
\arguments{
\item{tmp_matrix}{Numeric AD-TMP matrix with features in rows and samples in
columns.}

\item{sample_age}{Numeric sample ages. If named, names are matched to
\code{colnames(tmp_matrix)} after sample-ID normalization. If unnamed, the
vector must be in the same order as \code{colnames(tmp_matrix)}.}

\item{k_values}{Integer vector of segment counts to fit.}

\item{min_n}{Minimum number of samples allowed in each segment.}

\item{scale_rows}{Whether to center and scale AD-TMP rows before scoring
segments. Set this to \code{FALSE} when \code{tmp_matrix} has already been row-scaled.}
}
\value{
A list with \code{matrix}, \code{ordered_samples}, per-\code{k} \code{fits}, and a
\code{summary} table containing the segmentation score and mean silhouette width
for each candidate \code{k}.
}
\description{
Fit contiguous AD-TMP segmentations for a range of \code{k} values and return a
matrix whose rows are \code{k} values and whose columns are age-ordered samples.
This provides the data needed to draw a green age-ordered diagnostic heatmap
analogous to the manuscript age-class selection display, while preserving
contiguity for every fitted row.
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cpsa-protein.R
\name{ageTMP_cpsa_spec}
\alias{ageTMP_cpsa_spec}
\title{Create a CPSA model specification}
\usage{
ageTMP_cpsa_spec(
  time_col = "days",
  event_col = "os.status",
  feature_col = "feature_score",
  age_class_col = "age_class_new",
  age_reference = "ADULT",
  strata = c("PED", "ADO", "YA"),
  include_all_combined_test = TRUE,
  base_covariates = c("age", "Grade", "Cortical", "Midline"),
  adjustment_covariates = c("IDH1_mut", "TP53_mut", "ATRX_mut", "H33A_mut", "ATM_mut"),
  fdr_method = "BY",
  remove_sparse_covariates = TRUE,
  sparse_covariates = NULL,
  sparse_positive_level = 1,
  sparse_min_count = 3,
  sparse_count_method = c("numeric", "factor"),
  scale_features = TRUE,
  engine = c("coxph", "coxph.fit")
)
}
\arguments{
\item{time_col}{Column in \code{survival_data} containing survival time.}

\item{event_col}{Column in \code{survival_data} containing event status.}

\item{feature_col}{Temporary column name used for the current feature.}

\item{age_class_col}{Column containing age-class labels. Set to \code{NULL} with
\code{age_reference = NULL} and \code{strata = NULL} to fit a standard feature Cox
model without feature-by-age-class interactions.}

\item{age_reference}{Reference level for \code{age_class_col}.}

\item{strata}{Age strata to test by dropping feature terms.}

\item{include_all_combined_test}{Whether to add an omnibus \verb{ALL.comb.*}
likelihood-ratio test that drops the main feature and all feature-by-strata
interaction terms. Legacy two-level glyco ADO scripts did not create this
column.}

\item{base_covariates}{Clinical covariates included in every model.}

\item{adjustment_covariates}{Additional covariates, such as mutation calls.}

\item{fdr_method}{Multiple-testing adjustment method passed to
\code{\link[stats:p.adjust]{stats::p.adjust()}}.}

\item{remove_sparse_covariates}{Whether to drop binary covariates with very
few positive values in the current model frame.}

\item{sparse_covariates}{Optional character vector limiting which covariates
are eligible for sparse binary checks. If \code{NULL}, only covariates with no
more than two observed values are checked.}

\item{sparse_positive_level}{Positive value used for sparse binary checks.}

\item{sparse_min_count}{Drop sparse covariates with counts less than or equal
to this value.}

\item{sparse_count_method}{How to count sparse positive values. \code{"numeric"}
coerces binary/logical covariates to numeric before counting. \code{"factor"}
preserves the legacy manuscript behavior of counting
\code{table(factor(x, levels = c(0, 1)))[2]}, which drops logical covariates
such as \code{Cortical} and \code{Midline} in the glyco ADO scripts.}

\item{scale_features}{Whether to row-center and row-scale features inside
the model engine. Set to \code{FALSE} when the supplied matrix has already been
scaled in a manuscript-specific way.}

\item{engine}{Cox model fitting engine. \code{"coxph"} uses the formula-based
\code{\link[survival:coxph]{survival::coxph()}} path and is the default for manuscript replication.
\code{"coxph.fit"} uses the same model matrix with \code{\link[survival:agreg.fit]{survival::coxph.fit()}} to
reduce formula overhead in larger feature scans.}
}
\value{
A list describing the CPSA Cox model.
}
\description{
CPSA is feature-matrix agnostic: each row of \code{feature_matrix} is a molecular
feature or score, and each column is a sample. This specification object makes
the survival outcome, age-stratum interaction, and clinical/molecular
adjustment model explicit so users can adapt CPSA to different applications.
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cpsa-protein.R
\name{ageTMP_fit_reference_cpsa}
\alias{ageTMP_fit_reference_cpsa}
\title{Fit reference-cohort CPSA Cox models}
\usage{
ageTMP_fit_reference_cpsa(
  feature_matrix,
  survival_data,
  features = rownames(feature_matrix),
  spec = ageTMP_cpsa_spec(),
  trajectory_sd = NULL,
  trajectory_sd_min = NULL,
  trajectory_sd_feature_col = "feature",
  trajectory_sd_value_col = "sd",
  trajectory_sd_group_cols = NULL,
  trajectory_sd_keep = c("any_group", "all_groups"),
  n_cores = 1,
  progress = FALSE,
  progress_every = 500
)
}
\arguments{
\item{feature_matrix}{Numeric feature-by-sample matrix.}

\item{survival_data}{Prepared survival covariates.}

\item{features}{Optional subset of features to fit.}

\item{spec}{CPSA model specification from \code{\link[=ageTMP_cpsa_spec]{ageTMP_cpsa_spec()}}.}

\item{trajectory_sd}{Optional trajectory dynamic-range object used to screen
features before CPSA fitting. Accepted inputs are a named numeric vector of
per-feature trajectory standard deviations, a feature-by-age/sample matrix
whose row-wise standard deviation should be used, or a data frame such as
the output of \code{\link[=ageTMP_rank_trajectory_sd]{ageTMP_rank_trajectory_sd()}}.}

\item{trajectory_sd_min}{Minimum trajectory standard deviation required to
keep a feature. Use \code{NULL} to disable trajectory-SD screening.}

\item{trajectory_sd_feature_col}{Feature column in a \code{trajectory_sd} data
frame.}

\item{trajectory_sd_value_col}{Standard-deviation column in a \code{trajectory_sd}
data frame.}

\item{trajectory_sd_group_cols}{Optional grouping columns in a \code{trajectory_sd}
data frame. When supplied, \code{trajectory_sd_keep} controls whether a feature
must pass in all groups or any group.}

\item{trajectory_sd_keep}{Whether grouped trajectory-SD screening keeps
features passing in \code{"any_group"} or \code{"all_groups"}.}

\item{n_cores}{Number of parallel worker processes on non-Windows systems.}

\item{progress}{Whether to print fitting progress.}

\item{progress_every}{Number of features per progress chunk.}
}
\value{
A data frame with coefficients, p-values, adjusted FDR values, and
signed age-stratum statistics.
}
\description{
This is the modality-neutral CPSA model engine. It accepts any numeric
feature-by-sample matrix, including protein abundance, RNA expression,
glycopeptide abundance, phosphosite abundance, pathway scores, or other
molecular feature scores.
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/trajectory-tumor.R
\name{ageTMP_fit_tumor_trajectory}
\alias{ageTMP_fit_tumor_trajectory}
\title{Fit tumor age-dependent molecular trajectories}
\usage{
ageTMP_fit_tumor_trajectory(
  tumor_mat,
  tumor_metadata,
  features = rownames(tumor_mat),
  tumor_sample_col = "id",
  tumor_age_col = "age",
  tumor_sex_col = "sex",
  center_age_range = c(0, 26),
  fit_age_range = NULL,
  pre_scale = FALSE,
  span = 1.5,
  adaptive_span = FALSE,
  min_span = 0.5,
  max_span = 3,
  span_step = 0.1,
  prediction_ages = NULL,
  prediction_sample_ids = NULL,
  ci_level = 0.95,
  n_cores = 1,
  progress = FALSE
)
}
\arguments{
\item{tumor_mat}{Tumor feature-by-sample matrix.}

\item{tumor_metadata}{Tumor sample metadata.}

\item{features}{Features to model. Defaults to all matrix rows.}

\item{tumor_sample_col}{Tumor metadata sample ID column.}

\item{tumor_age_col}{Tumor metadata age column.}

\item{tumor_sex_col}{Tumor metadata sex column.}

\item{center_age_range}{Age range used to center and scale each feature.}

\item{fit_age_range}{Optional age range used for loess fitting.}

\item{pre_scale}{Whether to row-center and row-scale the tumor matrix before
the age-range centering step. This mirrors manuscript trajectory scripts
that first standardized the complete feature matrix, then standardized
again within the modeled age range.}

\item{span}{Loess span. May be a single number or a span data frame accepted
by the normal/tumor trajectory functions.}

\item{adaptive_span}{Recompute feature/sex-specific spans by GCV.}

\item{min_span}{Minimum span for adaptive selection.}

\item{max_span}{Maximum span for adaptive selection.}

\item{span_step}{Span grid step for adaptive selection.}

\item{prediction_ages}{Optional ages where trajectories are predicted. If
\code{NULL}, predictions are made at all harmonized tumor sample ages.}

\item{prediction_sample_ids}{Optional IDs corresponding to \code{prediction_ages}.}

\item{ci_level}{Confidence level for fitted trajectories.}

\item{n_cores}{Number of parallel worker processes for feature/sex fits on
Unix-like systems. Use \code{1} for serial execution.}

\item{progress}{Whether to print simple progress messages.}
}
\value{
A data frame with one row per feature, sex, and prediction age.
}
\description{
Fit sex-stratified age-dependent tumor molecular trajectories from a public
feature-by-sample matrix and sample metadata. This is the tumor-only AD-TMP
fitting step used by manuscript heatmap-style trajectory panels such as
Figure 2A.
}
\details{
The function intentionally mirrors key details from the manuscript tumor
trajectory workflow. Tumor sample IDs are harmonized with
\code{\link[=ageTMP_normalize_sample_ids]{ageTMP_normalize_sample_ids()}}, rows are centered/scaled using the requested
\code{center_age_range}, optional fitting can be restricted with \code{fit_age_range},
and sex-stratified \code{\link[stats:loess]{stats::loess()}} models are fit with base loess defaults.

For exact manuscript reproduction, the original protein workflow used
feature/sex-specific adaptive loess spans selected by generalized
cross-validation over a span grid. Set \code{adaptive_span = TRUE} to recompute
those spans from the public tumor matrix; this requires the suggested
\code{locfit} package and can be slow for thousands of proteins. A single numeric
\code{span}, or a data frame with \code{feature}, \code{sex}, \code{tissue}, and \code{span} columns,
can also be supplied when spans are known or when a faster reconstruction is
desired.
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tmp-clustering.R
\name{ageTMP_cluster_tmp_samples}
\alias{ageTMP_cluster_tmp_samples}
\title{Cluster samples from temporal molecular profiles}
\usage{
ageTMP_cluster_tmp_samples(
  tmp_matrix,
  k_max = 10,
  reps = 300,
  p_item = 1,
  p_feature = 1,
  algorithm = "km",
  distance = "euclidean",
  seed = 1234,
  title = "ageTMP_consensus_tmp",
  plot = NULL,
  use_consensus = TRUE
)
}
\arguments{
\item{tmp_matrix}{Numeric feature-by-sample TMP matrix.}

\item{k_max}{Maximum number of clusters to evaluate.}

\item{reps}{Number of consensus resampling repetitions.}

\item{p_item}{Sample resampling proportion for consensus clustering.}

\item{p_feature}{Feature resampling proportion for consensus clustering.}

\item{algorithm}{Clustering algorithm passed to \code{ConsensusClusterPlus}.}

\item{distance}{Distance metric passed to \code{ConsensusClusterPlus}.}

\item{seed}{Random seed.}

\item{title}{Output title/directory prefix used by \code{ConsensusClusterPlus}.}

\item{plot}{Plot option passed to \code{ConsensusClusterPlus}.}

\item{use_consensus}{Whether to use \code{ConsensusClusterPlus} when available.}
}
\value{
A list with the input \code{clustme}, cluster assignments by k, and the
raw consensus result when applicable.
}
\description{
Cluster samples using the stacked TMP matrix produced by
\code{\link[=ageTMP_combine_tmp_matrices]{ageTMP_combine_tmp_matrices()}}. When \code{ConsensusClusterPlus} is available,
this uses the consensus k-means workflow used by the manuscript clustering
scripts. Otherwise, it falls back to deterministic k-means for lightweight
development and testing.
}

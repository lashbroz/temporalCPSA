% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/supplement-age-class.R
\name{ageTMP_build_age_class_supplement}
\alias{ageTMP_build_age_class_supplement}
\title{Build the supplementary age-class table}
\usage{
ageTMP_build_age_class_supplement(
  clinical,
  tmp_matrix = NULL,
  sample_ids = NULL,
  id_col = "id",
  age_col = "age",
  breaks = c(0, 15, 26, 40, 62, 80),
  labels = c("PED", "ADO", "YA", "ADULT", "SEN"),
  right = TRUE,
  include_lowest = TRUE,
  ordered = TRUE
)
}
\arguments{
\item{clinical}{Clinical data frame.}

\item{tmp_matrix}{Optional feature-by-sample TMP matrix used to compute
sample silhouette widths.}

\item{sample_ids}{Optional sample IDs to include. If \code{NULL}, all clinical
samples with non-missing age are used, or the intersection with
\code{colnames(tmp_matrix)} when a TMP matrix is supplied.}

\item{id_col}{Clinical sample ID column.}

\item{age_col}{Clinical age column.}

\item{breaks}{Numeric breakpoints passed to \code{\link[base:cut]{base::cut()}}.}

\item{labels}{Age-class labels. Length must be \code{length(breaks) - 1}.}

\item{right}{Whether intervals are closed on the right.}

\item{include_lowest}{Whether the lowest age should be included in the first
interval.}

\item{ordered}{Whether to return an ordered factor.}
}
\value{
A data frame with \code{id}, \code{age.class}, \code{age.class.range}, and
\code{sil_width}.
}
\description{
Build the \code{cDisc_AgeClass}-style supplementary table from clinical ages and,
when available, the TMP matrix used for silhouette scoring. Age-class labels
and ranges are derived from numeric ages. \code{sil_width} is computed only when a
feature-by-sample TMP matrix is supplied.
}

# Load manuscript reference-cohort clinical data from STable1

Load manuscript reference-cohort clinical data from STable1

## Usage

``` r
ageTMP_load_cdisc_clinical(data_dir = "data")
```

## Arguments

- data_dir:

  Path to the public data directory.

## Value

A data frame containing the `ClinicalTable` sheet from `STable1.xlsx`.

# Load cDisc RNA matrix

Load cDisc RNA matrix

## Usage

``` r
ageTMP_load_cdisc_rna_matrix(data_dir = "data", collapse = TRUE)
```

## Arguments

- data_dir:

  Path to the public data directory.

- collapse:

  Whether to average rows with the same feature identifier.

## Value

A numeric gene-by-sample RNA matrix.
